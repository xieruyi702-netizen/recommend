<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rs.gateway.mapper.ItemMapper">

    <select id="selectNews" resultType="Item">
        SELECT id, title, tags, author, hot_score, ctr, publish_time, url, summary
        FROM items
        <if test="category != null and category != ''">
            WHERE tags LIKE CONCAT('%', #{category}, '%')
        </if>
        ORDER BY publish_time DESC
        LIMIT #{limit}
    </select>
</mapper>
